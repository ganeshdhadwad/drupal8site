/**
 * @file
 * Bootstrap Tooltips.
 */

var Drupal = Drupal || {};

(function ($) {
  "use strict";

  // var $id = $("#edit-field-heading-value").css('color','#ff0000');
  // alert($id);
  Drupal.behaviors.bootstrapCustom = {
    attach: function (context) {
      // Immediately return if tooltips are not available.
      // alert('WORKING');
    },
  };

})(window.jQuery);
